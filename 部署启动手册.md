# 约稿费用验收系统 · 完整部署启动手册

面向一台新机器，用 **uv** 把整套系统拉起来：LangGraph 工作流 + FastAPI `/api/v1` + React 前端。

包管理只认 `uv` + `pyproject.toml` / `uv.lock`。不要 `pip install`，不要用仓库里已经删掉的 `requirements-backend.txt` / `start-backend.sh`。

HTTP 合同以 `frontend/docs/API.md` 为准。后端内部流程见 `src/workflows/backend/DESIGN.md`。纯 CLI 跑批（不走网页）仍可用根目录 `DEPLOY.md`。

---

## 0. 系统长什么样

```
仓库根（所有后端 / CLI 命令的 cwd）
├── pyproject.toml / uv.lock / .python-version   # uv 环境
├── src/workflows/
│   ├── backend/          FastAPI（workflows.backend.main:app）
│   ├── nodes/            node_00 … node_06
│   ├── workflow_run.py   工作流入口
│   └── mcp_server.py     可选观测服务
├── frontend/             React + Vite（约稿平台）
├── table/                参考表 3 / 4 / 5（工作流读取）
├── table_test/           联调样例（含 1-链接.xlsx），不要当生产 table/
├── runtime/              自动创建：backend.db、uploads、workflow.db
├── output/{run_id}/      自动创建：约稿资料、付款表
├── runtime-mcp/          MCP 独立运行库（与 HTTP 后端隔离）
└── output-mcp/           MCP 产物（与 HTTP 后端隔离）
```

日常用法：浏览器打开前端 → 登录 → 上传表 1 → 后台跑节点 0–6 → 约稿资料 / 费用分析 / 下载文件。

| 服务 | 地址 | 说明 |
| --- | --- | --- |
| 后端 API | http://127.0.0.1:8000 | FastAPI |
| Swagger | http://127.0.0.1:8000/docs | 不鉴权的交互文档 |
| 健康检查 | `GET /health` → `{"status":"ok"}` | 部署探测 |
| 前端开发 | http://127.0.0.1:5173 | Vite，代理 `/api` → 8000 |
| MCP（可选） | http://127.0.0.1:8100/mcp | 工作流观测，默认不鉴权 |

---

## 1. 环境要求

| 项 | 要求 |
| --- | --- |
| OS | macOS / Linux。Windows 可用 WSL2 |
| Python | `>= 3.12`。仓库 `.python-version` 为 `3.14`，`uv` 会按这个拉解释器 |
| 包管理 | [uv](https://docs.astral.sh/uv/) |
| Node.js | 前端需要 **18+**（Vite 7）。用 `node -v` 确认 |
| 网络 | PyPI / npm / Playwright 浏览器源；节点 2 还要能打开待抓站点（知乎等） |
| 磁盘写权限 | 仓库根可写：`runtime/`、`output/`、`table/`、`.venv/` |

Linux 无桌面、又要抓知乎时：装 Xvfb（见第 12 节）。macOS 本机有图形界面即可。

---

## 2. 安装 uv

macOS / Linux：

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
export PATH="$HOME/.local/bin:$PATH"
uv --version
```

Homebrew：`brew install uv`。装完新开一个终端，确认 `uv --version` 有输出。

---

## 3. 拿到代码

```bash
git clone <本仓库地址> workflow
cd workflow
```

确认根目录有：`pyproject.toml`、`uv.lock`、`src/workflows/`、`frontend/`、`table/`。

后面所有 `uv run`、`uvicorn` 都在**仓库根**执行。不要 `cd src`。

---

## 4. 用 uv 安装 Python 与后端依赖

```bash
cd /path/to/workflow

# 若本机没有 .python-version 对应的解释器，uv 会下载
uv python install

# 按 uv.lock 创建 .venv 并装齐依赖（可复现）
uv sync
```

验证：

```bash
uv run python -c "import sys; print(sys.version); import fastapi, sqlalchemy, playwright, openpyxl; print('ok')"
```

说明：

- hatch 包装的是 `src/workflows`。启动 uvicorn 仍建议带 `PYTHONPATH=src`（本仓库验证过的方式）。
- 不要再 `pip install` 一份平行环境。
- `pytest` 未写入 `pyproject.toml`，日常跑系统不必装。

---

## 5. Playwright（节点 2 抓页面）

Python 包里的 `playwright` ≠ 浏览器。必须再装 Chromium：

```bash
uv run playwright install chromium
```

Linux 再装系统库（缺库时浏览器会秒崩）：

```bash
uv run playwright install-deps chromium
```

macOS 一般不需要 `install-deps`。没装浏览器时，节点 2 可能降级或失败，视站点而定。

---

## 6. 准备参考表 3 / 4 / 5

工作流启动时检查 `table/` 里这三张是否存在。缺文件会 critical 失败。**表 2、表 6 由 node_06 按固定模板生成**，配置接口不要求、也不接受这两张。

```
table/
├── 3-媒体库.xlsx      # 必填  kind=media_library
├── 4-账户信息.xlsx    # 必填  kind=accounts
└── 5-费用.xlsx        # 必填  kind=fee_rules
```

文件名必须带「3-媒体库」这类前缀，工作流按文件名找。仓库若已带样例，可直接联调；换正式数据只替换文件，不要改名。

表 1（`1-链接.xlsx`）**不要**放进 `table/` 当配置。HTTP 上传后落到 `runtime/uploads/{task_id}.xlsx`。联调样例在 `table_test/1-链接.xlsx`。

管理员也可登录后上传覆盖 `table/`：

```bash
# 先登录拿到 TOKEN，见第 9 节
curl -s http://127.0.0.1:8000/api/v1/config/files \
  -H "Authorization: Bearer $TOKEN" \
  -F kind=media_library \
  -F file=@./table_test/3-媒体库.xlsx
```

`GET /api/v1/config` 的 `all_ready=true` 只看磁盘上这三项是否存在。

---

## 7. 环境变量

路径类变量后端**会读**（`workflows.paths`）。不设则用仓库内默认。

| 变量 | 默认 | 说明 |
| --- | --- | --- |
| `WORKFLOW_RUNTIME_DIR` | `<repo>/runtime` | SQLite（`backend.db`、`workflow.db`）、表 1 上传 |
| `WORKFLOW_OUTPUT_DIR` | `<repo>/output` | 每次 run 的 xlsx，实际写入 `<dir>/<run_id>/` |
| `WORKFLOW_TABLE_DIR` | `<repo>/table` | 媒体库 / 账户 / 费用 |
| `WORKFLOW_INPUT_FILE` | `table/1-链接.xlsx` | **仅 CLI** 默认输入；HTTP 用上传路径，不用这个 |

根目录 `.env.example` 里的 `SECRET_KEY`、`BACKEND_HOST`、`FRONTEND_URL`、`ACCESS_TOKEN_EXPIRE_MINUTES` **当前代码并未读取**：

- JWT 密钥写死在 `src/workflows/backend/auth.py` 的 `SECRET_KEY`
- CORS 写死为 `http://127.0.0.1:5173`、`http://localhost:5173`
- access 7200 秒，refresh 7 天

上生产前必须改 `SECRET_KEY`，并把 CORS 改成真实前端 origin。改完重启 uvicorn。

前端（`frontend/.env`，可从 `frontend/env.example` 复制）：

```
VITE_API_BASE_URL=http://127.0.0.1:8000
```

开发时也可留空：Vite 把 `/api` 代理到 `127.0.0.1:8000`。生产构建时必须写成浏览器能访问到的后端地址。

---

## 8. 启动后端（uv）

数据库和种子账号在 **uvicorn 启动时自动创建**。`runtime/` 被清空后，下次请求也会 `ensure_db()` 重建表。

```bash
cd /path/to/workflow
PYTHONPATH=src uv run uvicorn workflows.backend.main:app --host 0.0.0.0 --port 8000
```

开发热重载：

```bash
PYTHONPATH=src uv run uvicorn workflows.backend.main:app --reload --host 0.0.0.0 --port 8000
```

可选手动灌库：

```bash
PYTHONPATH=src uv run python scripts/init_db.py
```

健康检查：

```bash
curl -s http://127.0.0.1:8000/health
# {"status":"ok"}
```

常见启动错误：

| 现象 | 处理 |
| --- | --- |
| `ModuleNotFoundError: workflows` | cwd 必须是仓库根，且带 `PYTHONPATH=src`。不要 `cd src` |
| `Address already in use` | 换端口，或杀掉占用 8000 的进程：`lsof -i :8000` |
| 循环导入 | `workflows.runtime` 已对 `start_workflow_job` 做惰性导入；仍要从仓库根启动 |

**SQLite + FastAPI BackgroundTasks：只用 1 个 worker。** 不要 `--workers 4`，多进程会抢同一任务、写坏 `backend.db`。

---

## 9. 种子账号

首次启动写入，已存在则不覆盖密码。

| 邮箱 | 角色 | 密码 |
| --- | --- | --- |
| admin@byd.local | admin | Passw0rd! |
| operator@byd.local | operator | Passw0rd! |
| finance@byd.local | finance | Passw0rd! |
| viewer@byd.local | viewer | Passw0rd! |

新注册用户默认 `operator` + `pending`，管理员在「用户管理」把 `status` 改成 `active` 后才能登录。

登录（curl）：

```bash
curl -s http://127.0.0.1:8000/api/v1/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"email":"admin@byd.local","password":"Passw0rd!"}'
```

成功体是 `{ "user": {...}, "tokens": { "access_token", "refresh_token", "token_type": "bearer", "expires_in": 7200 } }`。后续：

```
Authorization: Bearer <access_token>
```

公网不要用种子密码。

---

## 10. 启动前端

另开一个终端：

```bash
cd /path/to/workflow/frontend
cp env.example .env    # 首次；确认 VITE_API_BASE_URL
npm install
npm run dev
```

浏览器打开 http://127.0.0.1:5173 。用第 9 节账号登录。

页面与角色：

| 路由 | 页面 | admin | operator | finance | viewer |
| --- | --- | :---: | :---: | :---: | :---: |
| `/` | 首页概览 | ✓ | ✓ | ✓ | ✓ |
| `/processing` | 数据处理（上传表 1） | ✓ | ✓ | | |
| `/quotes` | 约稿资料（可切换历次处理） | ✓ | ✓ | ✓ | ✓ |
| `/analytics` | 费用分析 | ✓ | ✓ | ✓ | ✓ |
| `/exceptions` | 异常提醒 | ✓ | ✓ | | |
| `/exports` | 文件输出 | ✓ | ✓ | ✓ | |
| `/config` | 基础配置（表 3/4/5） | ✓ | | | |
| `/users` | 用户管理 | ✓ | | | |

生产构建：

```bash
cd frontend
npm run build          # 产出 frontend/dist
npm run preview        # 本地预览 dist，默认 4173
```

用 nginx 托管 `frontend/dist` 时：

1. 把 `VITE_API_BASE_URL` 设成浏览器能访问的 API 根（例如 `https://api.example.com`），再 `npm run build`（Vite 只在构建时打进包）。
2. 后端 CORS 必须包含该前端 origin（当前写死 5173，要改 `src/workflows/backend/main.py`）。
3. 也可让 nginx 把 `/api/` 反代到 uvicorn，前端 `VITE_API_BASE_URL` 留空，走同源。

---

## 11. 用页面跑通一条任务

1. 管理员确认「基础配置」三盏灯都是已配置（或 `GET /api/v1/config` 的 `all_ready=true`）。
2. 业务人员打开「数据处理」，上传 `1-链接.xlsx`。
3. 媒体名未全部命中媒体库 → 页面下拉改成标准名 → 重新校验。
4. 预检 `ready` 后前端会自动 `POST /run`。节点 2 抓网页可能几十秒，进度条按 7 个节点走。
5. `completed` 后：
   - 「约稿资料」下拉可切换**每一次**处理记录
   - 「费用分析」按发布日期所在月汇总（只含可入账明细）
   - 「文件输出」下载约稿资料 / 付款表 / zip

curl 等价路径（operator 或 admin）：

```bash
TOKEN=...
curl -s http://127.0.0.1:8000/api/v1/tasks/validate \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" \
  --data-binary @./table_test/1-链接.xlsx
# 记下 task_id；status 为 ready 时：
curl -s -X POST http://127.0.0.1:8000/api/v1/tasks/$TASK/run \
  -H "Authorization: Bearer $TOKEN"
# 轮询
curl -s http://127.0.0.1:8000/api/v1/tasks/$TASK \
  -H "Authorization: Bearer $TOKEN"
# 历次任务
curl -s http://127.0.0.1:8000/api/v1/tasks \
  -H "Authorization: Bearer $TOKEN"
```

表 1 必须是 **xlsx 二进制 body**（或 multipart 字段名 `file`），不是 JSON。

`status=completed` 且 `progress.completed_nodes` 长度为 7 即成功。产物在 `output/<run_id>/`。

---

## 12. 可选：CLI 直接跑工作流

不经过登录 / HTTP，在仓库根：

```bash
# macOS / 有桌面
uv run python -m workflows run --input table_test/1-链接.xlsx --table-dir ./table

# 无桌面 Linux
xvfb-run -a uv run python -m workflows run --input table_test/1-链接.xlsx --table-dir ./table
```

产物同样进 `output/<run_id>/`，运行库进 `runtime/workflow.db`。弱机可设 `WEB_SCRAPER_CONCURRENCY=1`。细节见根目录 `DEPLOY.md`。

Debian / Ubuntu 无桌面时：

```bash
sudo apt-get update
sudo apt-get install -y xvfb
```

---

## 13. 可选：MCP 观测服务

与 HTTP 后端**分目录**，不要共用 `runtime/` / `output/`。

```bash
PYTHONPATH=src uv run python -m workflows.mcp_server \
  --host 0.0.0.0 --port 8100 --path /mcp
```

默认运行库 `runtime-mcp/`、产物 `output-mcp/`。内网默认不鉴权，不要直接暴露公网。

---

## 14. 生产建议

- 改掉 `auth.py` 里的 JWT `SECRET_KEY`；种子密码只用于内网联调。
- uvicorn **单 worker**；用 systemd / supervisor 守护，`WorkingDirectory` = 仓库根。
- 备份 `table/`（参考表）和 `runtime/`（用户、任务、上传的表 1）。`output/` 是每次产物。
- 删 `runtime/backend.db` 会丢掉用户和任务元数据，下次启动会重建空库并重新写入种子账号；**不会**删 `table/` 里的 xlsx。
- CORS、前端 `VITE_API_BASE_URL`、反代 HTTPS 一起改，不要只改一边。
- 节点 2 依赖外网和 Chromium；防火墙要放行待抓站点。

systemd 示例（后端）：

```ini
[Unit]
Description=约稿平台 API
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/workflow
Environment=PYTHONPATH=src
ExecStart=/home/deploy/.local/bin/uv run uvicorn workflows.backend.main:app --host 127.0.0.1 --port 8000
Restart=on-failure

[Install]
WantedBy=multi-user.target
```

把路径换成实际位置。前端用 nginx 托管 `frontend/dist`，`/api/` 反代到 `127.0.0.1:8000`。

---

## 15. 故障排查

| 现象 | 处理 |
| --- | --- |
| `uv: command not found` | `export PATH="$HOME/.local/bin:$PATH"` |
| `requires-python >=3.12` | `uv python install` |
| 登录 422 | 打 `/api/v1/auth/login`，JSON 字段 `email` / `password`。`@byd.local` 可用 |
| 登录 401 | 密码错，或库被清空后要用种子密码重新登 |
| validate 400「尚未配置媒体库」 | `table/3-媒体库.xlsx` 不存在 |
| run 409 `TASK_CONFLICT` | 任务不是 `ready` |
| 工作流 `failed` | 看 `GET /tasks/{id}` 的 `error` 和 uvicorn 日志。典型：缺 3/4/5、表 1 解析失败、节点 critical |
| 知乎抓取失败 / 安全验证 | Linux 用 `xvfb-run`；不要强行 headless |
| 前端能开但接口跨域 | 确认前端 origin 在 CORS 列表；开发用 5173 |
| 前端空白 / 登录后无数据 | 后端没起，或 `VITE_API_BASE_URL` 指错；看浏览器 Network |
| zip 下载 500 | 已用 RFC 5987 处理中文文件名；确认后端是当前代码 |

---

## 16. 最小检查清单

1. [ ] `uv --version`、`node -v` 正常  
2. [ ] 仓库根 `uv sync` 成功，`.venv` 存在  
3. [ ] `uv run playwright install chromium`（Linux 再 `install-deps`）  
4. [ ] `table/` 中 3 / 4 / 5 三张表齐全  
5. [ ] `PYTHONPATH=src uv run uvicorn workflows.backend.main:app --host 0.0.0.0 --port 8000`  
6. [ ] `curl http://127.0.0.1:8000/health` → `{"status":"ok"}`  
7. [ ] `cd frontend && npm install && npm run dev`，浏览器 5173 能登录  
8. [ ] 上传 `table_test/1-链接.xlsx`，任务 `completed`，`output/<run_id>/` 有约稿资料和付款表  
9. [ ] 「约稿资料」能看到并切换该次处理记录  

全部完成后即可日常使用。更细的接口字段见 `frontend/docs/API.md`。
