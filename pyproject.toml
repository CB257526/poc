[project]
name = "workflow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "cb", email = "cb13551420764@163.com" }
]
requires-python = ">=3.14"
dependencies = [
    "langchain>=1.3.15",
    "langgraph>=1.2.11",
    "openai>=3.3.0",
]

[project.scripts]
workflow = "workflow:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
