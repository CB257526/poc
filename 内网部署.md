# 约稿费用验收系统 · 内网隔离部署

面向「服务器不能访问公网，只能按 IP:端口申请白名单」的公司内网。本机开发手册仍看 [部署启动手册.md](./部署启动手册.md)。

核心结论：**不要在内网服务器上 `uv sync` / `npm install`。** 在能上网、且与服务器同架构的 Linux 上打离线包，把产物拷进去；运行时只给节点 2 抓页面申请外网。

---

## 0. 这套系统在内网里长什么样

```
内网用户浏览器
        │
        ▼
   nginx :80（或公司入口）
        │
        ├─ /           → frontend/dist（静态文件，服务器上不需要 Node）
        ├─ /api/       → 127.0.0.1:8000
        └─ /health     → 127.0.0.1:8000/health

   uvicorn（单 worker，仓库根为 cwd）
        │
        ├─ runtime/backend.db     用户、任务
        ├─ runtime/workflow.db    工作流 checkpoint
        ├─ runtime/uploads/       上传的表 1
        ├─ table/                 3-媒体库 / 4-账户信息 / 5-费用
        ├─ output/{run_id}/       约稿资料、付款表
        └─ Playwright Chromium ──► 知乎 / 微博 / B 站 / 微信 / …（运行时外网）
```

前端在**有网的构建机**上 `npm run build`，服务器只托管 `dist`。Python + Chromium 必须在服务器上跑，因为节点 2 要真的打开链接。

LLM 未启用（`config.yaml` 里 `llm.enabled: false`），**不必**申请 Anthropic / OpenAI。

---

## 1. 先分清两类「外网」

| 阶段 | 要不要外网 | 典型目标 | 建议 |
| --- | --- | --- | --- |
| 安装依赖 | 要，但不要在生产机上做 | PyPI、npm、Playwright 浏览器、Python 解释器 | 在构建机一次性打好包，U 盘 / 堡垒机 / 内网文件柜拷进去 |
| 日常运行 | 只要节点 2 抓页面 | 媒体站点及其 CDN | 向网关申请**域名白名单**或正向代理；只开 IP 会反复失效 |

只申请「安装用」的 `pypi.org:443` 而不申请媒体站点：系统能打开、能登录，但上传表 1 后节点 2 会大面积 `SCRAPE_FAILED`。

只申请媒体站点、依赖靠离线包：这是正确路径。

---

## 2. 构建机要求（有网的那台）

必须和内网服务器 **OS + CPU 架构一致**。本仓库开发机若是 macOS，**不能**把 Mac 上的 `.venv` 拷到 Linux 服务器。

| 项 | 要求 |
| --- | --- |
| OS | 与生产相同（常见 RHEL / Rocky / Ubuntu） |
| 架构 | 与生产相同（`uname -m` → `x86_64` 或 `aarch64`） |
| Python | 3.12+（仓库 `.python-version` 为 3.14，构建机用 uv 拉同一版本） |
| Node.js | 18+（只在构建机需要；生产机不装 Node） |
| 磁盘 | 离线包大约 0.8–1.5 GB（含 Chromium） |

没有同架构 Linux 时：在 Mac 上用 Docker 按服务器架构构建（见第 4.B 节）。

先在构建机记下：

```bash
uname -s    # Linux
uname -m    # x86_64 / aarch64
cat /etc/os-release
```

生产机必须对得上。glibc 差太多（例如 Ubuntu 24.04 的包丢到 CentOS 7）会起不来，构建机尽量用同一发行版。

---

## 3. 运行时必须申请的外网（节点 2）

节点 2 用 Playwright 打开表 1 里的主链接，解析器覆盖：

知乎、微博、B 站、微信公众号/视频号、抖音、小红书、懂车帝、易车、今日头条、百家号、搜狐、快手；对不上的走 generic。

### 3.1 优先申请「域名 / 正向代理」，不要死磕 IP

这些站点走 CDN，IP 经常变，子域也多（静态资源、验证码、反爬脚本）。防火墙如果只认 IP:端口，过两周就会大面积超时。

向网管申请的推荐说法：

> 业务服务器 `10.x.x.x` 需要经公司正向代理访问下列 **HTTPS 443** 域名（媒体内容抓取，不访问其它公网）。请下发 HTTP/HTTPS 代理，或按域名放行。

常见域名（按实际表 1 再裁剪；没出现的平台可以先不申请）：

```
zhihu.com
zhuanlan.zhihu.com
static.zhihu.com
weibo.com
m.weibo.cn
bilibili.com
*.hdslb.com
mp.weixin.qq.com
weixin.qq.com
douyin.com
iesdouyin.com
xiaohongshu.com
xhscdn.com
dongchedi.com
yiche.com
toutiao.com
baijiahao.baidu.com
sohu.com
kuaishou.com
```

一个站点通常还会再拉一堆 CDN。最稳的是：

1. 网关给这台机器配 **HTTP 正向代理**（公司已有的 `http://proxy.corp:port`）。
2. 代理侧按域名放行上述站点。
3. 服务器环境变量指向该代理（见第 6 节）。

如果网管坚持「只给 IP」：让他们按域名解析后放行 `*:443`，并接受后续还要补单。先拿 **知乎 + 你们表 1 里实际出现最多的 2～3 个平台** 开通，联调通过再补。

### 3.2 不必申请的

- `pypi.org` / `npmjs.com` / `registry.npmmirror.com`（离线安装后运行时不用）
- `fonts.googleapis.com`（见第 8 节，构建时去掉）
- Anthropic / OpenAI（LLM 未启用）
- Playwright 下载源（浏览器打进离线包）

---

## 4. 在构建机打离线包

下面假设构建机已能访问 GitHub / PyPI / npm / Playwright（或公司镜像）。代码用 U 盘拷到构建机也行。

### 4.A 同架构 Linux 构建机

```bash
# --- 一次性工具 ---
curl -LsSf https://astral.sh/uv/install.sh | sh
export PATH="$HOME/.local/bin:$PATH"

# Node 18+ 用公司允许的方式装（二进制包 / nvm / 内网仓库）

cd /path/to/workflow
uv python install          # 按 .python-version 拉 3.14
uv sync --frozen           # 严格按 uv.lock
uv run playwright install chromium
# 若构建机也是无桌面 Linux，还要系统库：
# sudo uv run playwright install-deps chromium
```

前端在构建机编好（生产机不跑 `npm`）：

```bash
cd frontend
# 同源反代：不要把后端写成 http://127.0.0.1:8000
# 浏览器看到的是 nginx 的 host，/api 由 nginx 转到 uvicorn
printf 'VITE_API_BASE_URL=\n' > .env
npm ci
npm run build              # 产出 frontend/dist
```

打包（在仓库根）：

```bash
# Playwright 浏览器默认在 ~/.cache/ms-playwright
# 把它放到仓库内，方便整包拷贝
mkdir -p vendor
cp -a ~/.cache/ms-playwright vendor/ms-playwright

# uv 管理的解释器
# 常见路径：~/.local/share/uv/python/  或  ~/.local/share/uv/python/cpython-3.14.*
mkdir -p vendor/python
cp -a "$(uv python dir)" vendor/python-store
cp "$(command -v uv)" vendor/uv

tar -C .. -czf /tmp/workflow-offline.tar.gz \
  --exclude='.git' \
  --exclude='frontend/node_modules' \
  --exclude='__pycache__' \
  --exclude='.pytest_cache' \
  workflow
```

包内应有：源码、`uv.lock`、`.venv`、`frontend/dist`、`vendor/ms-playwright`、`vendor/uv`、`table/` 三张参考表。不要带 `runtime/`、`output/`（生产机自己建）。

`.venv` 里的 shebang 若写死了构建机绝对路径，到生产机要用第 5 节的 `uv sync --offline` 在生产机「就地」再链一次；所以 **wheels 离线源**更稳：

```bash
# 额外导出可再安装的 wheel 目录（推荐带上）
uv export --frozen --no-dev --no-hashes -o vendor/requirements.txt
uv pip download -r vendor/requirements.txt -d vendor/wheels --python .venv/bin/python
```

生产机即使 `.venv` 不能直接用，也能：

```bash
uv venv --python vendor/python-store/cpython-3.14-linux-x86_64-gnu/bin/python .venv
uv pip install --offline --no-index --find-links vendor/wheels -r vendor/requirements.txt
```

（具体 cpython 目录名以 `uv python dir` 为准。）

### 4.B 只有 Mac，服务器是 Linux：用 Docker 打 Linux 包

服务器若是 `linux/amd64`：

```bash
docker run --rm -it --platform linux/amd64 \
  -v "$PWD":/src -w /src \
  python:3.14-bookworm bash
```

容器内：

```bash
apt-get update && apt-get install -y curl xz-utils
curl -LsSf https://astral.sh/uv/install.sh | sh
export PATH="$HOME/.local/bin:$PATH"
uv sync --frozen
uv run playwright install --with-deps chromium
# 同样导出 vendor/wheels、ms-playwright
```

前端仍可在 Mac 上 `npm ci && npm run build`（JS 产物与 OS 无关），把 `frontend/dist` 放进离线包即可。

---

## 5. 内网服务器落地

假设解压到 `/opt/workflow`，运行用户 `deploy`。

```bash
sudo useradd -r -s /usr/sbin/nologin deploy   # 或沿用现有账号
sudo mkdir -p /opt/workflow
sudo tar -xzf workflow-offline.tar.gz -C /opt
sudo chown -R deploy:deploy /opt/workflow
cd /opt/workflow

# 若 .venv 不能直接用，按第 4 节用 vendor/wheels 重建
export PATH="/opt/workflow/vendor:$PATH"      # 里面有 uv 二进制
uv venv --python /opt/workflow/vendor/python-store/.../bin/python .venv
uv pip install --offline --no-index --find-links vendor/wheels -r vendor/requirements.txt
```

系统库（Debian/Ubuntu 无桌面）：

推荐方案：系统 Chrome（无需 Xvfb）

```bash
# 构建机打包时已下载 vendor/google-chrome-stable_current_amd64.deb
sudo apt-get install -y fonts-liberation fonts-noto-cjk
sudo apt install -y /opt/workflow/vendor/google-chrome-stable_current_amd64.deb
```

回退方案：Playwright Chromium（需 Xvfb）

```bash
sudo apt-get install -y xvfb fonts-liberation fonts-noto-cjk
# Playwright 缺的 .so：在构建机执行过 install-deps 的话，对照 ldd 补；
# 常见还有 libnss3 libatk-bridge2.0-0 libdrm2 libgbm1 libxkbcommon0
```

RHEL 系装 `google-chrome-stable_current_x86_64.rpm` 或 `xorg-x11-server-Xvfb` + 对应 nss/atk/gtk。

目录权限：`deploy` 对 `/opt/workflow` 可写（至少 `runtime/`、`output/`、`table/`、`logs/`）。

参考表：确认

```
table/3-媒体库.xlsx
table/4-账户信息.xlsx
table/5-费用.xlsx
```

文件名必须带这些前缀。表 1 不要放进 `table/`。

---

## 6. systemd + 环境变量

JWT 密钥目前写死在 `src/workflows/backend/auth.py`。上线前改成随机长串，并改掉种子密码（`src/workflows/backend/seed.py` 的 `DEMO_PASSWORD`）。改完必须重启。

`/etc/systemd/system/workflow-api.service`：

```ini
[Unit]
Description=约稿平台 API
After=network.target

[Service]
Type=simple
User=deploy
Group=deploy
WorkingDirectory=/opt/workflow
Environment=PYTHONPATH=src
Environment=PLAYWRIGHT_BROWSERS_PATH=/opt/workflow/vendor/ms-playwright
Environment=WEB_SCRAPER_CONCURRENCY=1
Environment=HTTPS_PROXY=http://proxy.corp.example:8080
Environment=HTTP_PROXY=http://proxy.corp.example:8080
Environment=NO_PROXY=127.0.0.1,localhost
# 无公司代理、网关已按域名直通时，删掉上面三行 PROXY
ExecStart=/opt/workflow/.venv/bin/uvicorn workflows.backend.main:app --host 127.0.0.1 --port 8000
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
```

要点：

- **`--host 127.0.0.1`**：只让 nginx 访问，不要把 8000 暴露到办公网。
- **不要 `--workers`**：SQLite + BackgroundTasks 只能单进程。
- **不需要 `xvfb-run`**：有系统 Chrome 时直接启动；用 Chromium 时才加（见下方旧方案）。
- `ExecStart` 用 `.venv/bin/uvicorn`，不要依赖 `uv` 在 PATH 里。
- 没有公司代理就不要设 `HTTPS_PROXY`，否则全站抓取都会去一个不存在的代理。

旧方案（Playwright Chromium + Xvfb）：

```ini
ExecStart=/usr/bin/xvfb-run -a /opt/workflow/.venv/bin/uvicorn workflows.backend.main:app --host 127.0.0.1 --port 8000
```

推荐直接用系统 Chrome 无头。

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now workflow-api
curl -s http://127.0.0.1:8000/health
# {"status":"ok"}
```

---

## 7. nginx：静态前端 + 同源反代

这样浏览器只访问一个 origin，**不必改 CORS**（当前 CORS 只放行了 5173，生产用同源刚好绕开）。

前端构建时 `VITE_API_BASE_URL` 必须为空，请求才会打到 `/api/v1/...`。

`/etc/nginx/conf.d/workflow.conf`：

```nginx
server {
    listen 80;
    server_name workflow.corp.example;   # 换成内网域名或 IP

    root /opt/workflow/frontend/dist;
    index index.html;

    client_max_body_size 50m;            # 上传 xlsx

    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 300s;         # 节点 2 抓页面可能数分钟
        proxy_send_timeout 300s;
    }

    location /health {
        proxy_pass http://127.0.0.1:8000/health;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

公司若统一在入口做 HTTPS，把证书配在那一层，这里保持 HTTP 即可。

```bash
sudo nginx -t && sudo systemctl reload nginx
```

浏览器打开 `http://workflow.corp.example`，用种子账号登录（内网联调可以；正式使用请改密码）。

---

## 8. 内网下前端字体

`frontend/index.html` 会请求 `fonts.googleapis.com` / `fonts.gstatic.com`。内网打不开时页面仍能用（CSS 已兜底 PingFang / 微软雅黑），只是英文字体变成系统默认。

上线前建议删掉那三行 Google Fonts `<link>`，避免浏览器一直挂起字体请求。需要 DM Sans 时，把字体文件放进 `frontend/src/assets/` 再 `@font-face`。

---

## 9. 备份与更新

| 路径 | 内容 | 策略 |
| --- | --- | --- |
| `table/` | 媒体库 / 账户 / 费用 | 定期备份；管理员在页面上传也会覆盖这里 |
| `runtime/` | 用户、任务、上传的表 1 | 定期备份；删 `backend.db` 等于清空账号和任务 |
| `output/` | 每次产物 | 按磁盘策略归档 |
| 源码 + `.venv` + `frontend/dist` | 程序 | 用新离线包覆盖；不要覆盖 `runtime/` / `output/` / `table/` |

更新流程：构建机打新包 → 停 `workflow-api` → 覆盖源码和 `frontend/dist` → 按需重建 `.venv` → 启动。不要在生产机 `git pull` 再 `uv sync`。

---

## 10. 验收

在内网服务器本机：

1. `curl -s http://127.0.0.1:8000/health` → `{"status":"ok"}`
2. 浏览器打开 nginx 地址，能看到登录页（字体可能是系统字体）
3. `admin@byd.local` / `Passw0rd!` 能进；「基础配置」三张表已配置
4. 上传 `table_test/1-链接.xlsx`（或业务真实表 1）
5. 任务能跑到 `completed`，`output/<run_id>/` 有约稿资料和付款表
6. 若节点 2 大量 `SCRAPE_FAILED`：在服务器上测代理/白名单

```bash
# 无代理、网关已放行时
curl -I https://www.zhihu.com
# 有公司代理时
curl -I -x http://proxy.corp.example:8080 https://www.zhihu.com
```

能返回 2xx/3xx 才说明抓取链路通。再看 uvicorn 日志里的 `scraping_success` / `scraping_failed`。

---

## 11. 故障对照

| 现象 | 处理 |
| --- | --- |
| `.venv/bin/python` 无法执行 / `cannot execute binary` | Mac 的 venv 拷到了 Linux，或架构不符。用 vendor/wheels 在生产机重建 |
| `Executable doesn't exist`（Playwright） | 方案 A：`google-chrome --version` 确认系统 Chrome；方案 B：`PLAYWRIGHT_BROWSERS_PATH` 没指到 `vendor/ms-playwright` |
| 浏览器秒崩、缺 `.so` | 补 Playwright 系统库；对照构建机 `install-deps` |
| 知乎标题是安全验证 / 空白 | 日志看 `browser_launched` 的 `channel`。`bundled-chromium` 被知乎拦，装系统 Chrome（第 5.1 节）；或代理把 JS/CDN 拦了 |
| 知乎仍需 Xvfb | 系统没装 Chrome，回退 Chromium headful。装 Chrome 即可无头：`sudo apt install ./google-chrome-stable_current_amd64.deb`，systemd 去掉 `xvfb-run` |
| 登录页能开，所有接口 CORS 报错 | 构建时把 `VITE_API_BASE_URL` 写成了 `http://127.0.0.1:8000`，浏览器在用户电脑上请求自己的 8000。重建 dist，变量留空 |
| 接口 404 | nginx 没反代 `/api/`，或 uvicorn 没起来 |
| 上传后一直 running、最后 failed | 看 journalctl：缺表 3/4/5、抓取超时、磁盘只读 |
| `Address already in use` | 8000 被占用 |
| 多开几个 uvicorn 后任务错乱 | 不要 `--workers`，不要开第二份同样的 service |

```bash
sudo journalctl -u workflow-api -f
```

---

## 12. 给网管的申请单模板

> 系统名称：约稿费用验收平台
> 服务器：`<内网 IP>`，系统账号：`<deploy>`
> 入站：办公网访问 `80/443`（经 nginx）；**不要**对办公网开放 `8000`、`8100`
> 出站（运行时）：经公司正向代理访问媒体站点 HTTPS 443（名单见第 3.1 节），用于抓取已发布稿件的标题/日期
> 出站（安装时）：无。依赖在隔离构建机打包后拷入
> 数据：业务表与用户库仅落在本机 SQLite，不访问公有云
