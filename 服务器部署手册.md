# 服务器部署手册

面向公司内网 Ubuntu（x86_64，已装 Docker，无外网、不能装 nginx）。本机 macOS 打 linux/amd64 镜像，拷到服务器 `docker load` 后启动。全部手工执行，仓库里没有部署脚本。

本机开发仍看 [部署启动手册.md](./部署启动手册.md)。纯 CLI 跑批看 [DEPLOY.md](./DEPLOY.md)。

---

## 0. 部署后长什么样

```
浏览器  →  宿主机 :80
              │
              ▼
     workflow-frontend  （Nginx + 前端静态文件）
              │  Docker 网络：http://backend:8000
              ▼
     workflow-backend   （FastAPI + Google Chrome，不对外暴露端口）
```

| 容器 | 镜像 | 宿主机端口 | 说明 |
| --- | --- | --- | --- |
| `workflow-frontend` | `workflow-frontend:v1.0` | `80` | Nginx 托管前端，`/api` `/health` `/docs` 反代到后端 |
| `workflow-backend` | `workflow-backend:v1.0` | 无 | FastAPI `:8000` + Chrome，只给前端容器访问 |

后端镜像里已经装好 **Google Chrome**（构建时验证过 `Google Chrome 152.0.7977.64`），知乎走无头 Chrome，不需要 Xvfb，也不需要在服务器上再装浏览器。

默认账号密码均为 `Passw0rd!`：`admin@byd.local`、`operator@byd.local`、`finance@byd.local`、`viewer@byd.local`。

---

## 1. 本机需要什么

- Docker Desktop（已开），并启用 `docker buildx`
- 仓库根有：`Dockerfile`、`Dockerfile.frontend`、`docker-compose.yml`、`nginx.conf`、`table/`、`frontend/`
- 前端构建：Node 18+

服务器：Ubuntu 24 x86_64、已装 Docker（`docker` + `docker compose` 或 `docker-compose`），账号示例 `yhgly@ps-aiplanf-gw1`。

---

## 2. 本机构建镜像

全部在仓库根执行。

### 2.1 前端静态文件

`VITE_API_BASE_URL` 必须留空：请求走相对路径 `/api/...`，由容器内 Nginx 反代。

```bash
cd frontend
printf 'VITE_API_BASE_URL=\n' > .env
npm install
npm run build
cd ..
```

确认存在 `frontend/dist/index.html`。

### 2.2 后端镜像（含 Chrome）

```bash
docker buildx build \
  --platform linux/amd64 \
  --tag workflow-backend:v1.0 \
  --file Dockerfile \
  --load \
  .
```

构建日志里应出现 `Google Chrome …`。

### 2.3 前端镜像（含 Nginx）

```bash
docker buildx build \
  --platform linux/amd64 \
  --tag workflow-frontend:v1.0 \
  --file Dockerfile.frontend \
  --load \
  .
```

### 2.4 核对

```bash
docker images | grep workflow
```

应看到：

```
workflow-backend    v1.0    …    约 688MB
workflow-frontend   v1.0    …    约 26MB
```

### 2.5 导出离线包

```bash
docker save workflow-backend:v1.0 workflow-frontend:v1.0 | gzip > workflow-images-v1.0.tar.gz
```

这个 gzip 大约 700MB，不要提交进 git。

---

## 3. 拷到服务器

本机：

```bash
scp workflow-images-v1.0.tar.gz yhgly@ps-aiplanf-gw1:~/
scp docker-compose.yml yhgly@ps-aiplanf-gw1:~/workflow/
scp -r table yhgly@ps-aiplanf-gw1:~/workflow/
```

`nginx.conf` 已经打进前端镜像，服务器上不必再拷。`Dockerfile` 也不必拷——服务器只 load 镜像，不在内网构建。

---

## 4. 服务器上手动启动

SSH 登录后：

```bash
ssh yhgly@ps-aiplanf-gw1
mkdir -p ~/workflow/runtime ~/workflow/output ~/workflow/screenshots
# 容器内用户 uid 1000，目录必须可写，否则 SQLite 报 unable to open database file
sudo chown -R 1000:1000 ~/workflow/runtime ~/workflow/output ~/workflow/screenshots
cd ~/workflow
```

加载镜像（只需一次；更新版本时再 load 一次）：

```bash
docker load < ~/workflow-images-v1.0.tar.gz
docker images | grep workflow
```

确认本目录有 `docker-compose.yml` 和 `table/3-媒体库.xlsx`、`table/4-账户信息.xlsx`、`table/5-费用.xlsx`。

启动（二选一，看服务器装的是哪个）：

```bash
docker compose up -d
# 或
docker-compose up -d
```

看状态：

```bash
docker ps
```

应有 `workflow-frontend`、`workflow-backend` 两个容器，frontend 映射 `0.0.0.0:80->80/tcp`。

---

## 5. 验证

```bash
curl http://localhost/health
# {"status":"ok"}

curl -sI http://localhost/ | head
# HTTP/1.1 200

docker exec workflow-backend google-chrome --version
# Google Chrome 152.x

docker exec workflow-frontend wget -q -O- http://backend:8000/health
# {"status":"ok"}
```

浏览器打开 `http://服务器IP/`，用 `admin@byd.local` / `Passw0rd!` 登录。

Swagger：`http://服务器IP/docs`。

---

## 6. 日常操作

都在 `~/workflow` 下：

```bash
docker logs -f workflow-frontend
docker logs -f workflow-backend

docker compose restart          # 或 docker-compose restart
docker compose down             # 停掉，数据在 runtime/ output/ 里还在
docker compose up -d

docker exec -it workflow-backend bash
docker stats
```

更新镜像：本机重新 build → `docker save` → `scp` → 服务器 `docker load` → `docker compose up -d`。`runtime/`（SQLite、上传文件）和 `output/`（约稿资料、付款表）是挂载目录，换镜像不会丢。

参考表 3/4/5 改了，直接覆盖服务器 `~/workflow/table/` 后重启后端即可：

```bash
docker compose restart backend
```

---

## 7. 外网白名单

容器本身不需要访问 PyPI / npm。节点 2 抓页面需要服务器能出网到这些站点（按公司流程申请域名或 IP）：

| 平台 | 域名 |
| --- | --- |
| 知乎 | `www.zhihu.com`、`zhuanlan.zhihu.com` |
| 微博 | `weibo.com`、`m.weibo.cn` |
| B 站 | `www.bilibili.com` |
| 微信 | `mp.weixin.qq.com` |
| 抖音 | `www.douyin.com` |
| 小红书 | `www.xiaohongshu.com` |
| 懂车帝 | `www.dongchedi.com` |
| 易车 | `news.yiche.com` |
| 今日头条 | `www.toutiao.com` |
| 百家号 | `baijiahao.baidu.com` |
| 搜狐 | `www.sohu.com` |
| 快手 | `www.kuaishou.com` |

LLM 未启用（`config.yaml` 里 `llm.enabled: false`），不必申请模型厂商。

---

## 8. 故障

**80 端口被占**

```bash
sudo ss -lntp | grep ':80'
```

改 `docker-compose.yml` 里 frontend 的 `"80:80"` 为 `"8080:80"`，然后 `docker compose up -d`。访问改成 `http://服务器IP:8080/`。

**`curl http://localhost/health` 返回 502，后端日志在 `Building workflow @ file:///app` / 拉 `hatchling` 超时**

内网访问不了 PyPI。旧镜像的启动命令是 `uv run uvicorn …`，容器一启动就会重新 build 本项目。`docker-compose.yml` 里已改成直接跑 `.venv/bin/uvicorn`，**不用重打镜像**，在服务器上把 compose 换成仓库里这一版后：

```bash
cd ~/workflow
docker-compose up -d
docker logs -f workflow-backend
curl http://localhost/health
# {"status":"ok"}
```

**前端能开、接口全失败**

```bash
docker exec workflow-frontend wget -q -O- http://backend:8000/health
docker logs workflow-backend
```

确认两个容器在同一个 compose 网络里，服务名必须是 `backend`（和 `nginx.conf` 里 `proxy_pass http://backend:8000` 一致）。

**任务报 `name 'Any' is not defined`（或其它 `NameError`）**

镜像内 Python 是 3.12，注解在定义时求值，漏导入就会炸；本机开发用 3.14 延迟求值，本地测不出来。修复后 `docker restart workflow-backend` 即可，**不要 `rm -f` / `up -d`**（那会从旧镜像重建，改动丢失）：

```bash
docker exec -u root workflow-backend sed -i \
  's/from typing import Dict, List, Mapping, Optional, Sequence/from typing import Any, Dict, List, Mapping, Optional, Sequence/' \
  /app/src/workflows/utils/web_scraper.py
docker restart workflow-backend
```

彻底修复：重打后端镜像时把基础镜像升到 `python:3.14-slim`，与本地 `.python-version` 对齐。

**知乎抓取失败**

```bash
docker exec workflow-backend google-chrome --version
docker logs workflow-backend | tail -100
```

没有 Chrome 输出 = 镜像不对，重新 load `workflow-backend:v1.0`。有 Chrome 但仍失败 = 多半是出网白名单没放行知乎。

**权限 / 数据库**

后端容器用户是 `workflow`（uid 1000）。宿主机 `runtime/` `output/` `screenshots/` 属主应对得上，否则 SQLite 报 `unable to open database file`，启动失败：

```bash
mkdir -p runtime output screenshots
sudo chown -R 1000:1000 runtime output screenshots
docker rm -f workflow-backend
docker-compose up -d
```

`ls -ld runtime` 应显示属主 `1000`。docker-compose 1.29 遇到 `'ContainerConfig'` 时不要 `up` 硬 recreate，先 `docker rm -f workflow-backend workflow-frontend` 再 `up -d`。

**不要**给后端加多个 uvicorn worker，SQLite 会被抢写。当前镜像 CMD 已是单进程。

---

## 9. 本机文件对照

构建机仓库根最终只需要这些部署相关文件（不含脚本）：

```
Dockerfile
Dockerfile.frontend
docker-compose.yml
nginx.conf
table/3-媒体库.xlsx
table/4-账户信息.xlsx
table/5-费用.xlsx
frontend/dist/          ← npm run build 产物，打进前端镜像
workflow-images-v1.0.tar.gz   ← docker save 产物，只拷服务器，不进 git
```
